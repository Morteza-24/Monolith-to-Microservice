precision modified works on everything
sr like precision
sm +=1 missing (no room for disscussion, they are different)
IFN look more for yourself
ned no decision yet (we should email them)
ICP modified works on everything


my thing for modifications to work on everything:
if microservices[0].isinstance(int):
    n_microservices = max(microservices)
else:
    n_microservices = max([max(_) for _ in n_microservices])
